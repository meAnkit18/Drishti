# 03.01 — simulation/ + config/ + api/ (physics core)

## api/ (3 py)

- `api/__init__.py` — 0 lines, package marker for `from api.route import safe_route` (`api/route_nowcast.py:23`).
- `api/route.py:1-31` — Flood-safe A* on 5 m grid. `safe_route(depth,valid,start,end,thresh=0.05)` (`:5`): `ok=valid&(depth<thresh)` (`:8`); heapq frontier (`:11`); 4×orth `5.0 m` + 4×diag `7.07 m` (`:17-18`, `dx=5` from `config/terrain.yaml:7`); Manhattan×5.0 heuristic (`:22-23`); returns `{path,length_m,blocked}` (`:10,26,31`). Consumes `simulate.py:104-105` depth or `planner/storms/*.bin` or ONNX `pred[li]`.
- `api/route_nowcast.py:1-61` — Live demo: `SPACE=https://aman34243-drishti-flood-nowcast.static.hf.space` (`:6`), `LEADS=[5,10,20,30,40,60,90,120,180]` (`:7`); `_get()` caches `path.replace("/","_")` + `urlopen(timeout=120)` (`:10-16`); `nowcast_route(base,cache,window,lead_min,start,end)` (`:19-20`) loads `windows/meta.json` (`:24`), `np.fromfile(dtype="<f4").reshape(1,channels,ny,nx)` (`:25-27`), fetches `drishti.onnx` + sidecar (`:28-32`), `ort.InferenceSession(CPU)` (`:33`), `pred=maximum(...,0)` (`:34`), `LEADS.index(lead_min)` (`:35-36`), `safe_route` (`:37`); returns `{scenario,t0,lead_min,max_depth_m,flooded_m2(*25),route_blocked,route_length_m,route_cells}` (`:38-42`); CLI `main()` (`:45-57`).

## config/ (5 YAML, no code)

- `config/drainage.yaml:1-29` — SYNTHETIC, `verified=false` (`:1`). `target_inlets:60, spacing:15.0` (`:2-3`); `placement{road_buffer:6.0, low bonus, avoid buildings}` (`:4-7`); `pipes{D:[0.3..1.2], default 0.6, trunk 1.0, n:0.013, S:[0.002,0.05], max_len:120}` (`:8-15`); `nodes{rim 0.0, depth 1.5, area 1.0, inlet_cap 0.05 m³/s}` (`:16-20`); `outfalls{2, NE low edge}` (`:21-23`); `uncertainty{D choices, n:[0.011,0.017], cap:[0.02,0.10], slope_noise 0.15, jitter 5.0}` (`:24-29`). Consumed `network.py:18,26-28,43,48,69-70,84-89`, `pipes.py:9-10`, `simulate.py:54-55`.
- `config/hydraulics.yaml:1-24` — NOT calibrated (`:1`). `surface{diffusive_storage_cell, h_min 0.001, h_flood 0.05, cfl 0.4, dt_fixed 2.0 s, dt_max 5.0 s, vel_cap 3.0}` (`:2-9`); `infiltration{horton road {5,1,2.0}, open {35,8,2.5}, building {0,0,1.0}; CN 98/78/98; dep 1.0/2.5/0.0 mm}` (`:10-20`); `pipes{surcharge_head 0.0, weir 1.7}` (`:21-22`); `blockage [0,0.10,0.25,0.50,0.75,0.90]` (`:23`). Consumed `simulate.py:52,60-65,72`, `infiltration.py:28-31`, `checks.py:9-10`.
- `config/rainfall.yaml:1-18` — daily max 114.59, p99 ~37 → events 10–150 mm 1–6 h (`:1-2`). `duration 1–6 h` (`:3-5`); `total 10–150 mm` (`:6-8`); `peak 90 mm/h` (`:9-10`); `step 5 min` (`:11`); temporals 5 + spatials 5 (`:12-13`); `moving speed [2,10] m/s dir [0,360]` (`:14-16`); `gaussian sigma [40,150] m` (`:17-18`). Consumed `generator.py:59-61,73-74,93-96`, `spec.py:10-14`, `suite_v2.py:96-97`.
- `config/simulation.yaml:1-23` — `simulation{dt_output 5 min, mass_tol 0.05, nan fail, v1.0}` (`:2-6`); `dataset{hdf5, out outputs/datasets, v1.0, test 10, val 100, prod 1000, v1_prod 240, v1_ood 36, seed 26085, nets 6, blockage balanced_quotas, rain stratified_lhs}` (`:7-19`); `validation{flood_cap 2.0 m, vel 3.0, quarantine 0.35}` (`:20-23`). Consumed `suite_v2.py:28-29,46`, `simulate.py:44,78`, `checks.py:4,14`, `twin.py:119` (seed).
- `config/terrain.yaml:1-27` — assumptions unless noted (`:1`). `domain{EPSG:32643 calc, EPSG:4326 store, 28.7523/77.4985, dx 5.0, nx 160 ~800 m, ny 110 ~550 m}` (`:2-9`); `dem{data/terrain_grid.json, native 30 m, acc 4 m, DSM, verified 13×20}` (`:10-15`); `landcover{road_half 4.0, building data/campus_accurate.geojson, road data/roads.geojson, boundary kiet_terrain/campus_osm.geojson ~83k m²}` (`:16-20`); `hydro{n road 0.015 open 0.045 bld 0.20, imperv 1.0/1.0/0.35}` (`:21-27`). Consumed `twin.py:84-93,95-99,123-152`.

## simulation/ (17 py: 7 empty __init__ + 10 logic)

- `simulation/terrain/twin.py:1-188` — Digital twin. `latlon_to_xy/xy_to_latlon` (`:14-18`, equirect `LAT0/LON0/M_LAT/M_LON` `:11-12`); `_point_in_poly` ray-cast (`:20-27`); `_load_polys` (`:29-51`); `_load_road_lines` first 500 (`:53-70`); `_dist_to_segments` (`:72-81`); `Twin(cfg)` (`:83-93`, `xs=(arange(nx)-nx/2+0.5)*dx`, row0=north `:87-88`); `_build_dem` (`:95-121`: 13×20 coarse `:99`, NaN→cKDTree `:105-109`, `zoom order=1` `:111`, crop/pad `:113-115`, micro-relief ±0.075 rng26085 `:119-120`); `_build_masks` (`:123-152`); `_build_hydro` gradient→D8 (`:158-174`)→accum (`:176-186`)→`low_points=top10%` (`:187-188`). Feeds network + simulate + checks.
- `simulation/drainage/network.py:1-98` — SYNTHETIC G(V,E) `verified=false`. `pipe_capacity(D,S,n=0.013)` (`:10-13`, A=πD²/4, R=D/4, (1/n)A R^2/3 √S); `generate(twin,cfg,seed,variant)` (`:15`, `rng(seed+variant*7919)` `:16`); `cand_score=accum+low*max+road_prox` (`:20-22`); mask `~domain|building→-1` (`:23`); greedy spacing (`:25-40`); jitter (`:43-46`); 2 lowest outfalls (`:56-61`); DAG link (`:65-83`, closer-to-outfall + max_len, trunk fallback `:82-83`); sample D/roughness (`:69-70,85-86`), slope clamp (`:87-89`), capacity (`:90`). → `pipes.py DrainageState` + `simulate.py:56`.
- `simulation/hydraulics/pipes.py:1-74` — 1D pipe/node. `DrainageState(network,cfg,blockage)` (`:5-23`); `step(dt_s,ponded,dx)` (`:25-74`) → `(sink_ms,return_ms)` (`:26-28,71-74`). `eff_cap=cap*(1-blk)` (`:29`); `eff_inlet*=(1-0.8*blk)` (`:31-35`); capture `min(eff_inlet, ponded*dx²/dt)`, ×0.3 if surcharged (`:37-46`); downhill routing (`:48-56`); `node_depth+=inflow*dt/1.0` (`:58`); `surcharge=head>0` (`:60-62`); return `q_back/(dx²)` mass-exact (`:64-69`); `sink=capture/(dx²)` (`:71-73`). Called `simulate.py:92-94`.
- `simulation/hydraulics/simulate.py:1-116` — Coupled loop. `apply_blockage(dst,level,mode,rng)` (`:7-19`); `simulate(twin,network,spec,hydro_cfg,rain_cfg,out_every,dem,manning,imperv_open,inlet_cap_scale,dep_scale)` (`:21-23`) → `{rain,depth,velocity,flooded,node_depth,pipe_flow/capacity,surcharge,overflow,time_to_flood,blockage,spec,mass{rain/infil/drain/discharged/stored/depression/ponded/maxd}}` (`:104-116`). Rain rescale (`:39-42`), recession tail (`:46-47,76-77`), substep dt/sdt (`:52-53`), inlet reload+scale (`:54-55`), Horton inline `kk=2.2` (`:59-65,83`), dep store (`:66-68,85-87`), loop (`:78-99`), TTF (`:98-99`), subsample (`:100-103`). Hub of everything.
- `simulation/rainfall/generator.py:1-96` — Seeded rain. `temporal_profile(kind,nt,rng)` (`:5-19`); `spatial_field(kind,X,Y,rng,cfg,spec)` (`:21-55`); `generate(spec,X,Y,cfg)` (`:57`, → `{rain[nt,ny,nx],dt_min,nt,spec}` `:89-90`); `random_spec` (`:92-96`). Moving advection `mx=cx+vx(t-T/2)` (`:79`); normalise total/nt (`:87`). Called `simulate.py:37`.
- `simulation/scenarios/spec.py:1-21` — Legacy suite. `BLOCKAGE_LEVELS=[0,0.10,0.25,0.50,0.75,0.90]` (`:4`); `make_suite(n,seed,cfg,variants=3)` (`:6`) → `{id,seed,temporal,spatial,duration,total,variant,blockage,mode}` (`:11-20`).
- `simulation/scenarios/suite_v2.py:1-314` — v1.0 stratified sampler. Versions 1.0 (`:28-29`); rain classes trace8/light12/moderate30/heavy30/extreme20 (`:32-38`); blockage quota (`:44`); N=6 (`:46`); `_lhs` (`:49-61`); `_assign_rain/blockage` (`:64-81`); `make_prod_suite` (`:84-144`, LHS dur/sigma/speed `:87`, edge 15% `:102-113`, drain_eff `:131-132`, jitter 0.05 `:136`, manning/dep/imperv `:138-140`, recession `:141`); `make_ood_suite` (`:147-178`); `assign_splits` 70/15/15 (`:181-215`); `make_topup_suite` ids10000+ (`:218-265`); `make_longdry_suite` ids20000+ (`:268-314`). Produces spec dicts for simulate.
- `simulation/surface/infiltration.py:1-41` — Horton+SCS-CN (DEAD, never instantiated). `horton_capacity(f0,fc,k,t)` (`:4-5`); `Infiltration(method,params,cell_class)` (`:7-22`, Smax=25400/CN-254 `:20`); `step(dt,rain)` (`:24-41`, Horton `:33`, SCS `:34-41`). Live path inlines instead.
- `simulation/surface/runoff.py:1-64` — Diffusive storage-cell (Bates & De Roo 2000). `step_surface(h,dem,manning,rain,infil,drain,dx,dt,h_min,blocked)` (`:8-11`) → `(h2,vel,Qin)` (`:64`). ws=dem+h (`:13`); E/S faces hf/S/q (`:17-32`, S≤0.05, Manning q=hf^5/3/n√S·dx); walls (`:33-38`); 1/8 limiter (`:41-46`); dh update (`:55`); vel proxy (`:62-63`, clip [0,3]). Called `simulate.py:94`.
- `simulation/validation/checks.py:1-52` — Gates. `check_surface(tol 0.05)` (`:4-12`: finite, ≥-1e-9, ≤2.0 m, vel ≤3.0); `check_mass(tol 0.35)` (`:14-22`); `check_blockage_effect` (`:24-26`); `check_lowpoints` 0.8× (`:28-33`); `check_graph` (`:35-52`: D∈[0.1,2], S∈[0.0005,0.2], orphans, outfall).
- `planner/storms/` — no py. `storm-a-val00213/meta.json` (t0 5, leads 10, 97.4 mm/4.86 h, blk 0.5) + `storm-b-test00224/meta.json` (t0 10, 92.5 mm/2.77 h, blk 0.75); each 10×`depth_*.bin` 35200 B = 110×160 int16 mm. Produced by `Aman34243/drishti-flood-nowcaster` (meta `model`), consumed by planner/viewer + `api/route.py`-style masking.
